# Paste into Streamlit Community Cloud → Manage app → Settings → Secrets
# NEVER commit real secrets.

OPENAI_API_KEY = "sk-your-key-here"
OPENAI_VISION_MODEL = "gpt-4o"
GOOGLE_DRIVE_FOLDER_ID = "1_ZkYcDg4zu42RKNN5o4ChipG7Wlz43Gs"

# Preferred (TOML-safe): run `python make_cloud_secrets.py` and paste those lines.
# GOOGLE_CREDENTIALS_B64 = "eyJ...."
# GOOGLE_TOKEN_B64 = "eyJ...."

# Avoid raw JSON in TOML on Cloud — quotes often make it "invalid toml".
